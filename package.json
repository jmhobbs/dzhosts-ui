{
  "name": "dzhosts-components",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "build": "npm run build --workspace @dzhosts/tokens && npm run build --workspace @dzhosts/components",
    "format": "oxfmt",
    "lint": "npm exec oxlint && npm exec -- oxfmt --check",
    "test": "vitest run",
    "storybook": "storybook dev -c packages/storybook/.storybook -p 6006",
    "build-storybook": "storybook build -c packages/storybook/.storybook"
  },
  "devDependencies": {
    "@storybook/addon-essentials": "^8.6.14",
    "@storybook/web-components-vite": "^8.6.14",
    "jsdom": "^26.1.0",
    "oxfmt": "^0.50.0",
    "oxlint": "^1.65.0",
    "storybook": "^8.6.14",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vitest": "^3.1.2"
  }
}
